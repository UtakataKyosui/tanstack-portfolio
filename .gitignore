node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Claude Code background-agent worktrees (local scratch state, not repo content)
.claude/
